name: Release
on:
  push:
    tags:
    - '*.*.*'
jobs:
  create_relese:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')